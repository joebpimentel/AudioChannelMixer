<UserControl x:Class="AudioChannelMixer.View.CompositeVolumeLevelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:view="clr-namespace:AudioChannelMixer.View"
             xmlns:viewModel="clr-namespace:AudioChannelMixer.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="120" d:DesignWidth="400">
    <UserControl.DataContext>
        <viewModel:CompositeVolumeLevelViewModel />
    </UserControl.DataContext>
    <Grid>
        <ListBox ItemsSource="{Binding Audios}">
            <ListBox.Items>
                <view:AudioVolumeLevelView />
            </ListBox.Items>
        </ListBox>
    </Grid>
</UserControl>
